
/*import React, { useState } from "react"

//send 

import SuperTokens, { SuperTokensWrapper } from; //"supertokens-auth-react"
// Suggested code may be subject to a license. Learn more: ~LicenseLog:1237737330.
import username, email, password from ; //where is locate the username,email,password
import Session from ;

SuperTokens.init({
  appInfo: {
    // learn more about this on https://supertokens.com/docs/references/app-info
    appName: ,//"<YOUR_APP_NAME>"
    apiDomain: ,//"<YOUR_API_DOMAIN>"
    websiteDomain: ,//"<YOUR_WEBSITE_DOMAIN>"
    apiBasePath: ,//"/auth"
    websiteBasePath: ,//"/auth"
  },
  recipeList: [username.init(),email.init(),password.init(), Session.init()],
});
 
class App extends React.Component {
  render() {
    return (
      <SuperTokensWrapper>
      </SuperTokensWrapper>
    );
  }
}*/